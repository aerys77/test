# 莉莉絲功能擴充紀錄

> 日期：2026-06-24
> 專案：莉莉絲聊天介面（Lilith Chat）

---

## 一、做了什麼

在**不修改 `chat.html` 任何一行**的前提下，為莉莉絲聊天介面新增了 5 個功能，全部動態注入到 `chat.js` + `chat.css`：

| # | 功能 | 實作方式 |
|---|------|---------|
| 1 | 📥 檔案下載 | 純瀏覽器 Blob API，零外部依賴 |
| 2 | 🔍 對話管理強化 | 搜尋過濾 + 雙擊重新命名 + 匯出，全 localStorage |
| 3 | 🔗 URL 閱讀 | Jina AI Reader API（`r.jina.ai/{url}`），打字貼網址自動偵測 |
| 4 | 🌐 網路搜尋 | Firecrawl Keyless Search API，開頭「幫我查/搜尋」觸發 |
| 5 | 🖼️ 圖片上傳 | FileReader → Base64 Data URI → Agnes 多模態格式 |

---

## 二、用了什麼工具 & 選用邏輯

### 2.1 外部 API

| 工具 | 用途 | 選用邏輯 |
|------|------|---------|
| **Jina AI Reader** | URL → markdown | **完全免費、免 API Key**。只要在 URL 前加 `https://r.jina.ai/` 就回傳乾淨 markdown，一行 `fetch` 搞定，零整合成本。 |
| **Firecrawl Keyless** | 網路搜尋 | 2026/6 剛推出 **免 API Key** 模式，每月 1000 次免費額度。內建搜尋引擎 + 爬蟲 + JS 渲染，一條 `POST` 就完成搜尋。 |
| **Agnes-2.0-Flash** | 聊天模型 | 支援 tool calling + streaming + 多模態，**永久免費**，相容 OpenAI API 格式。 |

### 2.2 被淘汰的方案與原因

| 被淘汰的方案 | 原因 |
|------------|------|
| DuckDuckGo API | 不穩定、需要繞過 rate limit，生產環境不可靠 |
| Google Custom Search | 要 API Key + 付費，對個人專案來說 overkill |
| Playwright 自爬 | 啟動成本高（2-3 秒），需要後端或 MCP server，95% 以上的公開頁面 Jina 都能讀，邊際效益低 |
| 圖片圖床 proxy（Option A） | 多一個後端依賴，Base64 直傳更簡單 |

### 2.3 開發工具

| 工具 | 用途 |
|------|------|
| **`read`** | 反覆讀取特定區塊確認插入點 |
| **`edit`** | 精準插入/修改程式碼，不整份重寫 |
| **`bash` + Node.js** | 語法檢查（`node -c`） |
| **`bash` + PowerShell** | 整合測試（函式存在性檢查、HTML 未修改驗證） |
| **Playwright** | 嘗試自動化瀏覽器驗證（環境限制未能成功，改採靜態語法分析替代） |

---

## 三、架構決策邏輯

```
全部不動 chat.html
       ↓
所有 UI 用 JS 動態注入（overlay + render pattern，與設定面板相同模式）
       ↓
輸入流程：純聊天，不新增按鈕
  ├─ 貼網址 → 自動讀取（Jina AI Reader）
  ├─ 開頭「幫我查/搜尋」→ Firecrawl 搜尋
  └─ 兩者同時 → URL 優先
       ↓
圖片：點既有 🖼️ toolbar 按鈕
  ├─ FileReader → base64 Data URI
  ├─ 預覽在輸入區上方
  └─ 送出格式：[{type:text}, {type:image_url}]
       ↓
儲存格式：新格式（陣列）與舊格式（字串）共存，無痛向後相容
```

---

## 四、功能速覽

```
▎📥 下載回覆
  AI 訊息 hover → ⬇️ 按鈕出現 → 點擊存為 .md

▎📥 下載整串對話
  對話 header header → ⬇️ 按鈕 → 存為 .json

▎🔍 搜尋對話
  側欄搜尋框打字 → 即時過濾對話列表

▎✏️ 重新命名
  雙擊對話標題 → 變輸入框 → Enter 儲存

▎🔗 讀取網頁（Jina AI Reader）
  輸入: 「https://example.com 摘要這篇」
  → 自動讀取網頁內容 → 莉莉絲根據內容回答

▎🌐 搜尋網路（Firecrawl Keyless）
  輸入: 「幫我查 2026 AI 工具」
  → Firecrawl 搜尋 → 莉莉絲根據搜尋結果回答

▎🖼️ 上傳圖片（Base64）
  點 🖼️ 選圖 → 縮圖預覽 → 送出
  → 莉莉絲看到圖片並回應
```

---

## 五、關於 Playwright 的紀錄

### 5.1 Playwright 能做的事

Playwright 可以做到 Jina AI Reader 和 Firecrawl 做不到的事：

| 場景 | Jina | Firecrawl | Playwright |
|------|:----:|:---------:|:----------:|
| 讀一般公開網頁 | ✅ | ✅ | ✅ |
| JS 渲染的 SPA | ✅ | ✅ | ✅ |
| 需要「按一下展開」才出現的內容 | ❌ | ❌ | ✅ |
| 需要登入後才能看的頁面 | ❌ | ❌ | ✅ |
| 表單填寫 + 送出後抓結果 | ❌ | ❌ | ✅ |
| 截圖 / 視覺確認 | ❌ | ❌ | ✅ |
| 監聽網路請求（XHR / fetch） | ❌ | ❌ | ✅ |
| 多步驟操作（登入→搜尋→點擊→爬） | ❌ | ❌ | ✅ |

### 5.2 為什麼現階段不加

1. **啟動成本高** — 每次要開瀏覽器實例，2-3 秒起跳，對聊天體驗是明顯延遲
2. **基礎設施複雜** — 在純前端 JS 裡跑 Playwright 不可行，需要後端伺服器或 MCP server
3. **邊際效益低** — 莉莉絲目前的兩個外部流程（讀網址、搜尋）都是**單次讀取**，不需要與頁面互動。Jina 和 Firecrawl 的 JS rendering proxy 已經夠用
4. **成本 vs 效益** — 為了那 <5% 讀不到的頁面，引入整套 Playwright 的維護成本不划算

### 5.3 建議的 escalation path

如果未來真的遇到 Jina 讀不到的頁面（空白、403、需要互動），考慮以下降級流程：

```
URL 閱讀流程（含 Playwright 降級）：

  收到網址
    ↓
  Jina AI Reader（0.5 秒）
    ↓
  成功？ → 回傳 markdown ✅
    ↓ 失敗（空白 / 403 / 需要互動）
  Playwright 啟動（2-3 秒）
    ↓
  用真實瀏覽器渲染 → 萃取內容
```

但這需要一個**後端 proxy** 來跑 Playwright（或透過 MCP server），目前不具備這個基礎設施。建議 **保留 Playwright 為已知選項，等真的遇到 Jina 讀不到的重要頁面時再補**。

---

## 六、檔案變更統計

| 檔案 | 原始 | 最終 | 新增 | 更動性質 |
|------|:---:|:---:|:----:|----------|
| `chat.js` | 929 行 | 1176 行 | **+247** | 新函式 + `sendMessage()` 改寫 + 渲染擴充 |
| `chat.css` | 522 行 | 580 行 | **+58** | 下載/編輯/預覽樣式 + light theme |
| `chat.html` | 155 行 | 155 行 | **0** | ✅ 完全未動 |
